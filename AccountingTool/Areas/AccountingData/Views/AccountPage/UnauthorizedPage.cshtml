@{
    Layout = "~/Views/Shared/_LayoutAccoutPage.cshtml";
}

<div id="app" class="text-center">
    <div class="container max-width-500">
        <h2>您已登出 或 登入已逾期</h2>
        <span id="countDownNumber">3</span>秒後即將切換至登入頁面
        <div class="spinner-border spinner-border-sm" role="status"></div>
    </div>
</div>
@section scripts{
    <script>
        // 清除 token cookie
        const date = new Date();
        date.setTime(date.getTime() - 1000);
        document.cookie = "accountingToolToken='';expires=" + date.toGMTString();

        function getCookie(name) {
            const value = `; ${document.cookie}`;
            const parts = value.split(`; ${name}=`);
            if (parts.length === 2) return parts.pop().split(';').shift();
        }
        console.log(getCookie("accountingToolToken"))

        // 倒數計時，轉至登入頁面
        // const countDownNumber = document.getElementById("countDownNumber")
        // setInterval(() => {
        //     const currentNumber = parseInt(countDownNumber.innerHTML);
        //     countDownNumber.innerHTML = currentNumber - 1
        //     if (currentNumber === 1) {
        //         window.location = window.location.origin + "/AccountingData/AccountPage/Login"
        //     }
        // }, 1000)
    </script>
}
